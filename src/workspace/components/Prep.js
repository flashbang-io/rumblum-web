import React from 'react';
import styled from 'styled-components';

export const SpaceWrap = styled.div`
  position: relative;
`;

const Plan = () => (<div />);

Plan.propTypes = {
};

Plan.defaultProps = {
};

export default Plan;
